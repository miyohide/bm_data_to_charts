<%= form_for(@experiment) do |f| %>
  <% if @experiment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@experiment.errors.count, "error") %> prohibited this experiment from being saved:</h2>

      <ul>
      <% @experiment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :experiment_at %><br>
    <%= f.datetime_select :experiment_at %>
  </div>
  <div class="field">
    <%= f.label :ruby_type %><br>
    <%= f.text_field :ruby_type %>
  </div>
  <div class="field">
    <%= f.label :note %><br>
    <%= f.text_area :note %>
 </div>
 <%= f.file_field :dstat_file %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
